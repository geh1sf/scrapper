name: Daily Kraimorie Property Tracker

on:
  schedule:
    # Run daily at 23:57 UTC (1:57 AM Bulgarian time)
    - cron: '57 23 * * *'

  # Allow manual triggering
  workflow_dispatch:

  # Run on push to main for testing
  push:
    branches: [ main ]

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  track:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
    - name: Checkout repository
      uses: actions/checkout@v5
      with:
        # Need full history to preserve previous results
        fetch-depth: 0

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install beautifulsoup4 requests lxml

    - name: Run daily Kraimorie tracking
      run: |
        echo "Starting daily Kraimorie property tracking..."
        python daily_tracker.py
      env:
        SENDGRID_API_KEY: ${{ secrets.SENDGRID_API_KEY }}
        FROM_EMAIL: ${{ secrets.FROM_EMAIL }}
        TO_EMAIL: ${{ secrets.TO_EMAIL }}

    - name: Commit results
      run: |
        git config --global user.name 'Kraimorie Tracker Bot'
        git config --global user.email 'action@github.com'
        git add data/ docs/
        git diff --staged --quiet || git commit -m "Daily Kraimorie tracking update $(date '+%Y-%m-%d %H:%M')"
        git push

    - name: Upload Pages artifact
      uses: actions/upload-pages-artifact@v3
      with:
        path: ./docs

    - name: Deploy to GitHub Pages
      id: deployment
      uses: actions/deploy-pages@v4